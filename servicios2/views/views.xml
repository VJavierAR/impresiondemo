<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="servicios.list">
      <field name="name">servicios list</field>
      <field name="model">servicios.servicios</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="servicios.action_window">
      <field name="name">servicios window</field>
      <field name="res_model">servicios.servicios</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="servicios.action_server">
      <field name="name">servicios server</field>
      <field name="model_id" ref="model_servicios_servicios"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="servicios" id="servicios.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="servicios.menu_1" parent="servicios.menu_root"/>
    <menuitem name="Menu 2" id="servicios.menu_2" parent="servicios.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="servicios.menu_1_list" parent="servicios.menu_1"
              action="servicios.action_window"/>
    <menuitem name="Server to list" id="servicios" parent="servicios.menu_2"
              action="servicios.action_server"/>
    -->
  </data>
</odoo>